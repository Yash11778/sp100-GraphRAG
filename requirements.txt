# Round 3 SP100 GraphRAG — pinned for reproducibility (Python 3.14)
# Core
python-dotenv==1.2.2
requests==2.34.2
numpy==2.5.1
pandas==3.0.3
tqdm==4.69.0
tiktoken==0.13.0

# API server
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.13.4

# HuggingFace Spaces entrypoint (app.py). The Docker SDK is a paid Spaces
# feature, so we deploy on the free Gradio SDK and mount our FastAPI app inside
# it -- every REST route keeps its path; Gradio only adds the /ui page.
gradio==6.20.0

# Generation (Gemini)
google-genai==2.12.1

# Embeddings + Traditional-RAG index (same embedding model for RAG & GraphRAG:
# sentence-transformers/all-MiniLM-L6-v2, 384-d)
fastembed==0.8.0
faiss-cpu==1.14.3

# Evaluation (brief-mandated BERTScore config: bert-score==0.3.13,
# model_type=roberta-large, rescale_with_baseline=True, idf=False)
bert-score==0.3.13
evaluate==0.4.6
transformers==5.14.1
torch==2.13.0
